Data folder is a data generated by SQLSpeedTest DBGenerator program (Data_large or Data_small)

specify any parameter for console mode:
ast.exe 1
or run r.bat

use #try directive to check for exceptions in both queries

use #Paradox, #Access, #Advantage, #DBISAM or #KeyDb to specify test engine

use #exec to run comand by calling ExecSQL; otherwise it will be opened by TDataset.Open method

do not forget to write select statement after DDL script or Insert, Update, Delete commands

ast_err.csv contains only errors
more detailed log is ast.csv





